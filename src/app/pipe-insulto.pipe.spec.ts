import { PipeInsultoPipe } from './pipe-insulto.pipe';

describe('PipeInsultoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeInsultoPipe();
    expect(pipe).toBeTruthy();
  });
});
