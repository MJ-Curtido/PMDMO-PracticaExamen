import { DirectivaRaraDirective } from './directiva-rara.directive';

describe('DirectivaRaraDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaRaraDirective();
    expect(directive).toBeTruthy();
  });
});
